# 词库导入格式

单词打卡支持导入 `.csv` 和 `.json` 文件。导入后，单词会保存到浏览器本地。

## CSV 格式

推荐使用表头：

```csv
word,meaning,example
abandon,放弃；抛弃,Do not abandon your plan.
accurate,准确的；精确的,The data must be accurate.
```

也支持中文表头：

```csv
单词,释义,例句
contribute,贡献；促成,Exercise contributes to health.
significant,重要的；显著的,This is a significant change.
```

如果没有表头，默认按下面顺序读取：

```csv
word,meaning,example
```

## JSON 格式

JSON 需要是数组，每一项包含 `word` 和 `meaning`，`example` 可选：

```json
[
  {
    "word": "analysis",
    "meaning": "分析",
    "example": "The analysis is convincing."
  },
  {
    "word": "approach",
    "meaning": "方法；接近",
    "example": "This approach works well."
  }
]
```

也支持中文字段：

```json
[
  {
    "单词": "benefit",
    "释义": "好处；受益",
    "例句": "Reading benefits your writing."
  }
]
```

## 注意事项

- `word` / `单词` 和 `meaning` / `释义` 必填。
- `example` / `例句` 可不填。
- 重复单词会自动跳过。
- 不建议一次导入特别大的文件；当前版本使用浏览器本地存储。
