# 联网公开博客改造步骤

这份文档用于把当前本地个人博客，逐步改造成可以公开访问、自己登录管理的联网博客。

## 目标形态

最终博客分为两个入口：

- 公开博客：访客可以阅读已发布文章、查看公开照片墙、访问关于我页面。
- 管理后台：只有自己登录后才能写文章、管理照片墙、打卡、背单词、设置主题和备份数据。

建议公开和私有内容这样划分：

| 类型 | 公开给访客 | 只给自己 |
| --- | --- | --- |
| 已发布文章 | 是 | 是 |
| 草稿文章 | 否 | 是 |
| 标签 / 分类 | 是 | 是 |
| 公开照片墙 | 是 | 是 |
| 关于我 | 是 | 是 |
| 打卡任务 | 否 | 是 |
| 番茄钟记录 | 否 | 是 |
| 单词背诵记录 | 否 | 是 |
| 设置 / 备份 | 否 | 是 |

## 阶段 1：拆分公开页和后台页

目标：先让项目有“公开博客”的页面形态。

需要做：

- 新增公开首页，用于展示博客名称、签名、文章列表、标签筛选。
- 新增公开文章阅读页，只展示已发布文章。
- 新增公开照片墙展示页，只读展示照片墙。
- 新增公开关于我页面。
- 保留当前工作台作为后台入口。

建议入口：

```txt
/public 或 /blog    公开博客
/admin 或 /studio   管理后台
```

完成标准：

- 不登录也能看到公开博客页面。
- 公开页面不显示写作、打卡、单词、设置等后台功能。
- 后台仍然保留当前所有本地功能。

## 阶段 2：整理数据层

目标：为联网同步做准备，避免页面代码直接到处操作本地存储。

需要做：

- 把当前的 `api.save()`、`api.load()` 扩展为统一数据层。
- 增加文章、任务、单词、照片墙等独立操作函数。
- 以后页面只调用数据层，不直接关心数据来自本地还是服务器。

建议形式：

```js
blogStore.getPosts()
blogStore.savePost(post)
blogStore.deletePost(id)
blogStore.getPublicPosts()
blogStore.uploadImage(file)
blogStore.sync()
```

完成标准：

- 页面逻辑不直接依赖 localStorage / IndexedDB 的细节。
- 后续可以在数据层里接入后端 API。

## 阶段 3：增加后端服务

目标：让博客有可以联网访问的数据接口。

推荐技术：

```txt
Node.js + Express
```

需要做：

- 创建后端项目。
- 提供文章、用户、设置、照片墙、文件上传等 API。
- 前端通过 HTTP 请求访问后端。

建议接口：

```txt
GET    /api/public/posts
GET    /api/public/posts/:id
GET    /api/public/profile
GET    /api/public/photo-walls

POST   /api/auth/login
POST   /api/auth/logout

GET    /api/admin/posts
POST   /api/admin/posts
PUT    /api/admin/posts/:id
DELETE /api/admin/posts/:id

POST   /api/admin/uploads
```

完成标准：

- 后端可以启动。
- 前端能从后端读取文章列表。
- 管理后台能通过接口保存文章。

## 阶段 4：增加数据库

目标：把数据从浏览器本地保存，改为服务器数据库保存。

起步推荐：

```txt
SQLite
```

后期可升级：

```txt
PostgreSQL
```

建议数据库表：

```txt
users
posts
tags
post_tags
media
photo_walls
photos
settings
tasks
focus_records
words
word_review_records
```

第一批优先入库：

- users
- posts
- tags
- media
- settings

第二批再入库：

- photo_walls
- photos
- tasks
- focus_records
- words
- word_review_records

完成标准：

- 文章数据保存在数据库中。
- 刷新页面或换设备后，仍然能读取服务器上的文章。

## 阶段 5：增加登录系统

目标：公开博客任何人能看，后台只有自己能进。

需要做：

- 创建管理员账号。
- 增加登录页。
- 登录成功后保存 token。
- 后台接口校验登录状态。
- 未登录访问后台时跳转登录页。

推荐方式：

```txt
账号密码 + JWT token
```

完成标准：

- 未登录不能进入后台。
- 登录后可以进入后台并管理文章。
- 公开博客不需要登录即可访问。

## 阶段 6：改造图片和文件上传

目标：公开博客中的头像、背景、照片墙图片、文章配图不再只存在本地。

需要做：

- 新增上传接口。
- 后端保存文件。
- 数据库保存文件 URL。
- 前端使用 URL 显示图片。

起步方案：

```txt
服务器 /uploads 文件夹
```

后期可升级：

```txt
对象存储，例如 S3、七牛云、阿里云 OSS、Supabase Storage
```

完成标准：

- 后台能上传图片。
- 公开页面能正常显示图片。
- 图片不再依赖浏览器本地缓存。

## 阶段 7：文章发布流程

目标：让文章具备正式博客需要的发布状态。

文章状态建议：

```txt
draft      草稿
published  已发布
private    私密
archived   归档
```

文章字段建议：

```js
{
  id,
  title,
  summary,
  content,
  cover,
  status,
  visibility,
  tags,
  createdAt,
  updatedAt,
  publishedAt
}
```

完成标准：

- 草稿不会出现在公开博客。
- 已发布文章会出现在公开博客。
- 后台可以编辑、发布、撤回文章。

## 阶段 8：公开博客阅读体验优化

目标：让访客看到的是一个真正适合阅读的博客，而不是后台工具。

需要做：

- 优化公开首页布局。
- 优化文章阅读页排版。
- 增加标签 / 分类筛选。
- 增加文章搜索。
- 增加上一篇 / 下一篇。
- 增加公开关于我页面。
- 保留二次元风格，但减少对正文阅读的干扰。

完成标准：

- 访客能顺畅浏览文章。
- 手机和电脑上都能正常阅读。
- 公开页面加载速度较快。

## 阶段 9：部署上线

目标：让博客可以通过公网访问。

方案 A：省心云服务

```txt
前端：Vercel / Netlify
后端：Supabase / Render / Railway
数据库：Supabase PostgreSQL
文件：Supabase Storage
```

优点：

- 不需要自己维护服务器。
- 有免费默认网址。
- 上手快。

缺点：

- 后期自由度略低。
- 某些平台免费额度有限。

方案 B：自己掌控

```txt
服务器：云服务器
后端：Node.js
数据库：SQLite 起步，后期 PostgreSQL
文件：服务器 uploads 文件夹
反向代理：Nginx
HTTPS：Let's Encrypt
```

优点：

- 自由度高。
- 所有功能都能自己控制。

缺点：

- 需要维护服务器、安全、备份。

完成标准：

- 公开博客可以通过公网地址访问。
- 后台可以登录管理。
- 数据能稳定保存。

## 阶段 10：同步私有功能

目标：把当前博客里的个人工具也同步到云端。

建议顺序：

1. 打卡任务和子任务。
2. 番茄钟专注记录。
3. 单词词库和背诵记录。
4. 主题设置、背景音乐、背景图。
5. 数据备份和恢复。

完成标准：

- 换电脑登录后，仍然能看到自己的任务、单词、专注记录。
- 私有数据不会出现在公开博客。

## 推荐执行顺序

建议后续按这个顺序推进：

1. 新增公开博客页面骨架。
2. 抽出统一数据层。
3. 创建 Node.js 后端。
4. 增加数据库，先同步文章。
5. 增加登录系统。
6. 增加图片上传。
7. 完善文章发布流程。
8. 优化公开阅读页。
9. 部署上线。
10. 同步打卡、番茄钟、单词等私有功能。

## 当前最适合先做的任务

第一步建议先做：

```txt
新增公开博客页面骨架
```

原因：

- 风险最低。
- 不影响当前本地后台功能。
- 能马上看到公开博客的形态。
- 后续接后端时改动更清晰。

